apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-app.fullname" . }}-preview
spec:
  selector:
    {{- include "devops-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}