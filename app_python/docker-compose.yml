services:
  app:
    image: blazz1t/devops_app:latest
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - SERVICE_NAME=devops-info-service
      - VERSION=1.0.0
      - DEBUG=false
